# ตัวอย่างที่ 3: การใช้งาน Code Generation (Tab)
# พิมพ์ comment อธิบายสิ่งที่ต้องการ แล้วกด Tab เพื่อให้ AI generate โค้ด

"""
วิธีใช้ Code Generation:
1. พิมพ์ comment อธิบายสิ่งที่ต้องการ
2. กด Enter เพื่อขึ้นบรรทัดใหม่
3. รอให้ AI แสดงข้อเสนอแนะ (สีเทา)
4. กด Tab เพื่อยอมรับ หรือ Ctrl+→ เพื่อยอมรับเฉพาะคำถัดไป

เทคนิคการเขียน comment ที่ดี:
- ใช้ภาษาไทยหรือภาษาอังกฤษก็ได้
- อธิบายให้ละเอียดและชัดเจน
- ระบุ input และ output ที่ต้องการ
"""

# ลองพิมพ์ comment เหล่านี้แล้วกด Tab:

# ฟังก์ชันที่ตรวจสอบว่าอีเมลถูกต้องหรือไม่


# ฟังก์ชันที่อ่านไฟล์ JSON และคืนค่าเป็น dictionary


# คลาสสำหรับจัดการฐานข้อมูล SQLite


# ฟังก์ชันที่ส่ง HTTP GET request และคืนค่าเป็น JSON


# ฟังก์ชันที่แปลง datetime เป็น string รูปแบบ "วัน/เดือน/ปี"


# ฟังก์ชันที่คำนวณระยะทางระหว่างจุดสองจุดบนแผนที่


# คลาสสำหรับจัดการ configuration settings


# ฟังก์ชันที่สร้าง password แบบสุ่มตามความยาวที่กำหนด


# ฟังก์ชันที่บีบอัดไฟล์ด้วย gzip


# ฟังก์ชันที่ส่งอีเมลผ่าน SMTP


"""
เคล็ดลับ:
- ถ้า AI generate โค้ดไม่ตรงตามต้องการ ให้แก้ไข comment ให้ละเอียดขึ้น
- สามารถใช้ # TODO: เพื่อให้ AI รู้ว่าต้องทำอะไรต่อ
- ใช้ docstring เพื่อให้ AI เข้าใจบริบทมากขึ้น
"""